/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PhotosCasePipe } from './photos-case.pipe';

describe('PhotosPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotosCasePipe();
    expect(pipe).toBeTruthy();
  });
});
